/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Cornerstone",
  assetPrefix: "/Cornerstone",
  images: { unoptimized: true },
};

module.exports = nextConfig;
